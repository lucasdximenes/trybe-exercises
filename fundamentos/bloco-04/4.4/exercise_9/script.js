function higherNum(firstNum, secondNum) {
    if (firstNum > secondNum) {
        return firstNum + " é maior que " + secondNum;
    } else {
        return secondNum + " é maior que " + firstNum;
    }
}
